{
  "name": "@sitey/siteyctl",
  "version": "0.1.0",
  "private": true,
  "description": "Control a Sitey server over HTTPS with an API token",
  "engines": {
    "node": ">=24"
  },
  "type": "module",
  "bin": {
    "siteyctl": "src/main.ts"
  },
  "scripts": {
    "start": "node src/main.ts",
    "test": "node --test tests/**/*.test.ts",
    "test:types": "tsc --noEmit"
  },
  "dependencies": {
    "@trpc/client": "^11.0.0"
  },
  "devDependencies": {
    "@trpc/server": "^11.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3",
    "zod": "^3.23.8"
  }
}
