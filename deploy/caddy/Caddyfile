:80 {
    @api path /trpc* /webhook* /health*

    handle @api {
        reverse_proxy sitey-api:3001
    }

    handle {
        root * /srv
        encode gzip
        try_files {path} /index.html
        file_server
    }
}
